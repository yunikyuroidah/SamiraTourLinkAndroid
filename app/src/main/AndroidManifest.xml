<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.samiratravelmobile">

    <!-- Internet & network permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Optional: untuk Glide / image caching -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.SamiraTravelMobile"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- 🔹 Launcher Activity (Halaman Utama Scroll - User) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🔹 Admin Login -->
        <activity
            android:name=".AdminLoginActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- 🔹 CRUD Paket, Galeri, Profil -->
        <activity
            android:name=".EditPackageActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
        <activity
            android:name=".EditGalleryActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
        <activity
            android:name=".EditProfileActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
        <activity
            android:name=".EditTourLeaderActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Firebase initialization -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/logo" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/teal_700" />
        <meta-data
            android:name="com.google.firebase.database.DatabaseUrl"
            android:value="https://samira-travel-810df.firebaseio.com" />
        <meta-data
            android:name="com.google.firebase.analytics.application"
            android:value="true" />

    </application>
</manifest>
